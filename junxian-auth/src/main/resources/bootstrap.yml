server:
  port: 8081
  servlet:
    context-path: /auth

spring:
  application:
    name: junxian-auth
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        namespace: public